# Progress

- Moved writing off the homepage onto `/blog/`. The personal note now includes “I have a blog” as the path there, and Identity Cage remains the writing document with its Medium link. Verified at 752 by 1014 and 390 by 844 with no homepage writing section, no horizontal overflow, a clean Astro check, and production builds at both supported base paths.
- Removed the “That’s the short version.” lead from the values work path, set “See more work” in Aleo, and gave the link a muted dotted underline.
- Restored swipe-to-refresh on the homepage without bringing back the native rubber band. A committed top pull that holds near the spring limit now reloads on release, covering mobile Safari and the macOS 27 trackpad gesture that `overscroll-behavior-y: none` would otherwise block. Casual bounces, reversed gestures, and the bottom edge do not reload. Reduced motion restores native overscroll so the platform refresh remains available without the decorative displacement.
- Reworked the custom elastic edges into a connected Essence-palette curtain whose ridges blend together instead of sitting as separate slabs. Imperial Blue through Magenta travels along the pull, Alice Blue is omitted so it cannot cut a white hole, and the document backing stays Imperial Blue during a top pull so the join cannot flash the off-white field. Display height tracks the pull so the field stretches and contracts with the spring, and the tips fade into the page.
- Expanded the opening flipbook from eight to eleven authored frames. Frames 9–11 now render in numeric order, share the existing scroll range through dynamic frame-count timing, and extend the four field-color cuts without changing the hard-scroll boundary or dissolve handoff. Verified a slow pass reaches every frame from 1 through 11, while 12,000-pixel gestures remain bounded at 1,437 pixels in the default viewport and 1,687 pixels at 390 by 844, with no mobile overflow or browser warnings.
- Gave Work and Let’s talk a muted dotted underline at rest. It fades with the label when the drawing appears on hover, keyboard focus, or an open contact disclosure.
- Set the values premise and the non-interactive words in each switching phrase to italic regular Crimson Text, keeping Bright Chalk upright on the cycling word.
- Restored “Three things important to me.” to the identity composition after the elastic overscroll transform captured its fixed carrier and positioned it above the viewport. The homepage transform now exists only while an elastic edge is active, preserving both the values handoff and the pull effect. Verified the restored heading at the reported 820 by 999 viewport with zero horizontal overflow, zero Astro diagnostics, and successful production builds at both `/` and `/personal-website/` base paths.
- Replaced the homepage's native vertical rubber band with a custom top-and-bottom elastic response. Outward input now moves the whole page to expose an Essence-palette field behind it, so the effect extends the document without covering content or adding scroll height. Visual iteration replaced the earlier repeated lumps, marbled surface, and hard ray silhouettes with one continuous low-resolution canvas per edge: elongated ridged noise creates soft vertical aurora currents whose thermal color follows the pull direction. The texture stays at its natural height and the moving page reveals it progressively, so it does not compress into linear bands. The response holds for 280 milliseconds and returns with a damped spring.
- Removed the top compositing hairline by snapping page displacement to physical device pixels and retaining an Imperial Blue backing throughout the top pull. Recording review exposed a colored strip during the final spring pixels; the field now dissolves into its matching backing before those last pixels while the spring remains active through exact zero. The bottom uses the matching off-white backing. The spring and normal scrolling stay uncancelled. Each edge is a single static canvas rendered only on connect/resize and softly blurred once; inactive edges are hidden, and every custom edge listener remains passive.
- Verified the natural top release and reverse-scroll handoff at 1440 by 900 and 390 by 844, the matching bottom treatment, exact viewport width with no horizontal overflow, fully dormant edge layers after settling, and an empty browser warning/error log. Astro check reported zero diagnostics, and production builds completed at both `/` and `/personal-website/` base paths.
- Widened only the identity summary measure from 31rem to 35rem, leaving the hello, headline, navigation, and overall 36rem introduction composition unchanged.
- Earlier native elastic-edge experiments were superseded by the custom displacement above after visual review showed that browser rubber-band surfaces could not be controlled consistently.
- Added a small amount of breathing room around the centered Work and contact pair: slightly more separation from the identity summary and a gentler gap before the carried values premise.
- Restored the introduction's original 36rem container, 24ch headline, and 31rem summary measure after exploring wider alternatives; the centered Work and contact treatment remains unchanged.
- Restored native elastic overscroll during an earlier iteration. This was later superseded on the homepage by the custom edge response above; page shells still clip only horizontally, and the opening wheel guard remains attached only during the bounded box-opening phase.
- Rebuilt the awkward side-mounted Work and contact stack as a quiet pair centered directly after the introduction copy, then repeated it as a centered closing gesture at the bottom of the page. Work and Connect drawings are absent at rest and resolve through a reversible blur as their labels dissolve on hover or keyboard focus, with no hover underline and no collision with the introduction; secondary documents retain a compact fixed text navigation.
- Verified the navigation with Igor's photograph open at 1440 by 900, the Work and contact blur states, Escape dismissal, the expanded narrow-screen disclosure at 390 by 844, zero horizontal overflow, no browser warnings, a clean Astro check, and production builds at both supported base paths.
- Renamed the identity thermal-word rAF loop so it no longer shadows `HTMLElement.animate`, restoring a clean `astro check`.
- Removed the opening “Scroll to open” prompt. The lid now teaches the mapping by lifting within the first 20–40px of scroll.
- Shortened the opening flipbook's closed first frame so the lid begins lifting within the first 20–40px of scroll instead of waiting through about 19% of a viewport. Remaining frames keep even spacing through the rest of the box-opening range.
- Split the homepage into OpeningSequence, IdentityIntroduction, ValuesEvidence, and WritingPreview, with copy in `src/content/homepage/` and named motion modules.
- Replaced inverted portfolio tokens with field, ink, copy, muted, and accent.
- Removed WorldField, HomeReturn, paper grain, and the previous hiring landing-page components.
- Rebuilt `/` as one composition: identity without a work CTA, values evidence as objects in the field, and writing as the next passage.
- Rebuilt `/work/` and independent articles onto the same field and type language.
- Replaced the identity `experiences` click colouring with a semantic-text-backed canvas thermal field shaped for Averia Serif. Staggered, domain-warped metaballs grow from each letter center, blend continuously between glyphs, move from pink through dark blue, then reverse and clear back to the untouched text.
- Verified the thermal wave’s ignition, overlapping forward travel, per-letter reverse, clean black resting state, complete glyph coverage including descenders, replay trigger, and 1440, 390, and 320 layouts with no horizontal overflow.
- Aligned the thermal canvas to the DOM text baseline and split its input behavior: touch/click plays the complete traveling wave, while fine-pointer hover continuously moves a local liquid field with the cursor and softly releases on exit.
- Added a one-time, shortened thermal afterglow as the identity finishes emerging from the opening essence, while preserving the full-strength interactive variants.
- Replaced the typeset identity hello with the authored 18-frame handwritten sequence. It plays once as the identity becomes visible and holds the completed frame for reduced motion and no-JavaScript reading.
- Verified the handwritten sequence from its first playing frame through its completed hold at 1440 by 900, 1280 by 720, 390 by 844, and 320 by 800, with no horizontal overflow or browser warnings. The production compiler also completed at both root and `/personal-website/` base paths.
- Reduced the handwritten hello, opened its vertical crop so Igor's `g` descender remains fully visible, and moved the role label directly beneath it to leave the adjacent space open. Verified the stack at 1440 by 900 and 390 by 844 with no horizontal overflow.
- Tightened the identity summary to replace `desire` with `ideas` and state the same thought more directly.
- Removed the drawn portrait and the portrait switcher. The identity now rests centered with no visible photo; activating `Igor` in the handwritten hello layers a smaller photograph into the upper-right whitespace without moving the copy, with an accessible show/hide control.
- Verified the centered default state and the stacked photo reveal at 1440 by 900, 820 by 999, and 390 by 844. Production compilation completed at both root and `/personal-website/` base paths.
- Gave every values-evidence screenshot a stable slight random tilt and a rounded white frame.
- Removed the supporting paragraph from each sticky value heading, and replaced each evidence title and body with a short body-font caption, keeping the Observatory case-study link.
- Set `--font-reading` to Aleo and `--font-special` to Bright Chalk, leaving Averia for headlines.
- Removed the underline from the cycling value words.
- Set cycling value words to inherit heading color and size to the active word so the line reflows instead of holding a reserved width.
- Added a velocity-bounded follower and a three-viewport runway to the opening sequence. Slow scrolling remains closely coupled to native progress, while fast wheel and trackpad input advances quickly without skipping the box opening in one paint.
- Replaced the scroll-position correction loop with an early wheel-boundary guard. It consumes forward overshoot only while the box opens and pigment expands, without repeatedly repositioning the document against browser momentum.
- Used the supplied 11-second recording to identify a compositor overshoot that could cross the sticky boundary before the next wheel event. Added a one-shot scroll fallback for that first crossed frame; subsequent momentum is absorbed without repeated correction.
- Reduced pigment-field main-thread work by rendering at a maximum 96-pixel internal dimension and about 30 frames per second, and by skipping the second field sample outside the terminal echo. The canvas keeps its deliberately low-resolution blurred presentation while leaving more time for scroll processing.
- Extended the opening to four viewports without stretching its animation. The box and essence retain two scrollable viewports, then the cleared identity receives one complete resting viewport before values can enter. Fast-gesture correction now stops at the animation boundary, preserving that identity runway for the next deliberate scroll.
- Shortened the guard to end at opening progress 0.7, when the final box frame is visible and the essence is near maximum spread. The essence clear and identity reveal no longer keep scroll locked; scrolling back before progress 0.56 rearms the early guard.
- Verified the shortened guard with a 14,000-pixel gesture at 820 by 999: it released while the final frame and near-full essence were visible, before the identity reveal completed; the next forward scroll moved natively through the identity runway, and returning to the closed box correctly rearmed the early guard.
- Fixed the colored-field scroll-out mismatch by separating capped and native phases. The first two scrollable viewports remain velocity-bounded through progress 0.7; the next viewport maps native scroll directly from progress 0.7 to 1 for the essence dissolve and identity reveal; a fourth scrollable viewport holds the completed identity before values enter.
- Verified the separated phases at 820 by 999 with an extreme post-release gesture: reveal opacity tracked physical scroll from 0.05 to 0.21 to 0.99, then the essence cleared and opening reached its completed off-white state before the sticky section left the viewport. At 390 by 844, an extreme gesture likewise finished with the essence hidden, reveal opacity 1, and no horizontal overflow. Also fixed the one-pixel boundary tolerance that could otherwise leave the early guard stuck below progress 0.7.
- Fixed hard macOS trackpad flicks escaping the early opening boundary. Forward wheel distance is now intercepted from the first event, reapplied one-to-one, and clamped at progress 0.7 while the visual follower catches up. Continued momentum is consumed only at that boundary, and opposite-signed inertia no longer releases it unless the document actually scrolls backward. Verified at 820 by 999 that repeated high-intensity input held the document at 1,997 pixels with Values 2,998 pixels below the viewport, released at the final blue frame, completed the dissolve before the sticky surface left, and rearmed after scrolling back below the reset point.
- Set the identity colour word to Bright Chalk with a dotted underline.
- Removed the identity colour-word interaction. `joyful` is now ordinary summary copy set in Bright Chalk.
- Rejected the high-contrast torn-paper treatment on the cycling Values words after visual review. The active word now remains black and uses a quiet muted-ink dotted underline as its interaction cue, consistent with the identity treatment. Verified the word cycle at 1440 by 900 and 390 by 844 with no horizontal overflow or browser warnings.
- Brought the values premise onto the revealed identity screen by pinning the heading to the lower field while the opening still covers the viewport, then handing it to the sticky values voice. Verified at 1440 by 900 with the heading visible beside the identity copy without further scrolling, and hidden during the unopened box.
- Removed the opaque rectangle exposed by that carried values premise and raised its resting position from the viewport edge. The same transparent voice now travels continuously from the identity composition into the values evidence instead of reading as a separate card or section surface. Verified the resting composition and moving handoff at 1440 by 900, 820 by 719, and 390 by 844 with no horizontal overflow or browser warnings.
- Set non-interactive values voice text in Crimson Text, keeping Bright Chalk on the cycling word.
- Replaced the diagonal clipped values handoff with a stationary blur transition. Each phrase now softens out completely, switches at the midpoint, and resolves back into focus without overlapping text or vertical movement.
- Added a restrained transient colour bloom to that blur using the opening essence palette. Imperial Blue ink, Cloudy Sky and Tiger Flame fringes, and a thin Magenta haze appear only while the letters are out of focus, then clear back to crisp black.
- Verified the stationary essence-colour handoff at 1440 by 900 and the values layout at 390 by 844 with no horizontal overflow or browser warnings. Production compilation completed at both root and `/personal-website/` base paths.
- Increased the transition pigment visibility after review showed the first treatment compositing into neutral grey. The phrase now retains opacity through the colourful part of the blur, with more saturated essence ink and post-blur chromatic shadows before its final disappearance.
- Changed the transient ink from one blended tint to an essence gradient across the phrase, carrying Imperial Blue through Cloudy Sky, Mustard, Tiger Flame, and Magenta while blurred, with an all-black gradient at rest.
- Verified the clearly visible multi-colour blur at 1440 by 900 and its black resting state and layout at 390 by 844, with no horizontal overflow or browser warnings. Production compilation completed at both supported base paths.
- Softened the essence gradient after visual review: each swatch is now mixed with the page field, post-blur saturation and chromatic shadows are restrained, and opacity falls earlier through the handoff so the colour reads as a light wash rather than a heavy band.
- Verified the lighter wash at 1440 by 900 and the black resting state and layout at 390 by 844 with no horizontal overflow or browser warnings. Production compilation completed at both supported base paths.
- Removed the values colour treatment after aesthetic review. Reworked the handoff into a continuous black-ink blur morph: outgoing and incoming phrases crossfade with complementary curves, overlap only while defocused, and share an optically centered position so the next phrase grows directly from the previous shape instead of appearing after a blank gap.
- Lowered the sticky values voice from the upper field to a more vertically centered rest so it sits in the open space between evidence objects. The identity pin still begins in the lower field and travels to that same resting position. Verified the rest at 1440 by 900 and 390 by 844 with no horizontal overflow.
- Verified the exact 50/50 morph crossover at 1440 by 900, the resolved word interaction, and subpixel-aligned layer centers at 390 by 844 with no horizontal overflow or browser warnings.
- Production compilation for the black-ink morph completed at both root and `/personal-website/` base paths.
