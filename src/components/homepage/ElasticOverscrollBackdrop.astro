<elastic-overscroll-backdrop aria-hidden="true">
  <div class="elastic-overscroll-backdrop" data-elastic-layer data-placement="top">
    <div class="elastic-overscroll-backdrop__backing"></div>
    <div class="elastic-overscroll-backdrop__glow">
      <canvas class="elastic-overscroll-backdrop__field"></canvas>
    </div>
    <div class="elastic-overscroll-backdrop__seam"></div>
  </div>

  <div class="elastic-overscroll-backdrop" data-elastic-layer data-placement="bottom">
    <div class="elastic-overscroll-backdrop__backing"></div>
    <div class="elastic-overscroll-backdrop__glow">
      <canvas class="elastic-overscroll-backdrop__field"></canvas>
    </div>
    <div class="elastic-overscroll-backdrop__seam"></div>
  </div>
</elastic-overscroll-backdrop>

<script>
  import './elastic-overscroll-backdrop';
</script>

<style>
  .elastic-overscroll-backdrop {
    position: fixed;
    z-index: 0;
    right: 0;
    left: 0;
    display: none;
    overflow: hidden;
    contain: strict;
    opacity: 0;
    pointer-events: none;
    visibility: hidden;
  }

  .elastic-overscroll-backdrop[data-placement='top'] {
    top: 0;
  }

  .elastic-overscroll-backdrop[data-placement='bottom'] {
    bottom: 0;
  }

  :global(.js) .elastic-overscroll-backdrop {
    display: block;
  }

  .elastic-overscroll-backdrop[data-edge-active] {
    z-index: 2;
    opacity: 1;
    visibility: visible;
  }

  .elastic-overscroll-backdrop__backing {
    position: absolute;
    right: 0;
    left: 0;
    background: var(--color-field);
    transform: scaleY(0);
  }

  [data-placement='top'] .elastic-overscroll-backdrop__backing {
    top: 0;
    transform-origin: center top;
  }

  [data-placement='bottom'] .elastic-overscroll-backdrop__backing {
    bottom: 0;
    transform-origin: center bottom;
  }

  .elastic-overscroll-backdrop__glow {
    position: absolute;
    right: 0;
    left: 0;
    filter: blur(clamp(0.7rem, 1.8vw, 1.2rem)) saturate(90%);
    transform: scaleY(0);
  }

  [data-placement='top'] .elastic-overscroll-backdrop__glow {
    top: 0;
    transform-origin: center top;
  }

  [data-placement='bottom'] .elastic-overscroll-backdrop__glow {
    bottom: 0;
    transform-origin: center bottom;
  }

  .elastic-overscroll-backdrop[data-edge-active] .elastic-overscroll-backdrop__backing,
  .elastic-overscroll-backdrop[data-edge-active] .elastic-overscroll-backdrop__glow,
  .elastic-overscroll-backdrop[data-edge-active] .elastic-overscroll-backdrop__seam {
    will-change: transform;
  }

  .elastic-overscroll-backdrop__seam {
    position: absolute;
    right: 0;
    left: 0;
    opacity: 0;
    -webkit-backdrop-filter: blur(clamp(1rem, 2vw, 1.5rem)) saturate(108%);
    backdrop-filter: blur(clamp(1rem, 2vw, 1.5rem)) saturate(108%);
    -webkit-mask-image: linear-gradient(
      to bottom,
      transparent 0%,
      #000 32%,
      #000 68%,
      transparent 100%
    );
    mask-image: linear-gradient(
      to bottom,
      transparent 0%,
      #000 32%,
      #000 68%,
      transparent 100%
    );
  }

  [data-placement='top'] .elastic-overscroll-backdrop__seam {
    top: 0;
  }

  [data-placement='bottom'] .elastic-overscroll-backdrop__seam {
    bottom: 0;
  }

  .elastic-overscroll-backdrop[data-edge-active] .elastic-overscroll-backdrop__seam {
    opacity: 1;
  }

  .elastic-overscroll-backdrop__field {
    position: absolute;
    right: -1.25rem;
    left: -1.25rem;
    display: block;
    width: calc(100% + 2.5rem);
    height: 100%;
  }

  [data-placement='top'] .elastic-overscroll-backdrop__field {
    top: 0;
  }

  [data-placement='bottom'] .elastic-overscroll-backdrop__field {
    bottom: 0;
  }

  @media (prefers-reduced-motion: reduce) {
    :global(.js) .elastic-overscroll-backdrop {
      display: none;
    }
  }
</style>
