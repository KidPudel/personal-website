---
import AnchorRedirect from '../../../components/shell/AnchorRedirect.astro';
import { sitePath } from '../../../config/site';

const redirectTarget = `${sitePath('/work/')}#games`;
---

<AnchorRedirect target={redirectTarget} destination="Game work" />
