---
import AnchorRedirect from '../components/shell/AnchorRedirect.astro';
import { sitePath } from '../config/site';

const redirectTarget = `${sitePath('/')}#site-contact`;
---

<AnchorRedirect target={redirectTarget} destination="Contact" />
