---
import AnchorRedirect from '../components/shell/AnchorRedirect.astro';
import { sitePath } from '../config/site';

const redirectTarget = sitePath('/#side-projects');
---

<AnchorRedirect target={redirectTarget} destination="Side projects" />
